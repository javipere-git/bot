# Credenciales: NUNCA subir tokens
config/credentials.ini

# La lista de bloqueadas DEL BROKER es de cada PC: es distinta en cada broker
# (Alpaca 863, Tastytrade 394) y se rehace con el boton "Traer del broker".
# Si viajara con el repo, chocaria en cada actualizacion. Tus propias excluidas
# (config/excluidas.txt) SI van al repo y te siguen a las tres PCs.
config/excluidas_broker.txt
*.env
.env

# Python
__pycache__/
*.pyc
.venv/
venv/

# Logs (incluidos los rotados: registro_....log.1, .log.2, ...)
*.log
*.log.*
registro_*
logs/

# Reportes de las pasadas del bot (se generan solos; no van al repo)
reportes/

# Codigo de referencia de Marian (propiedad de el; solo referencia visual)
referencia_marian/
